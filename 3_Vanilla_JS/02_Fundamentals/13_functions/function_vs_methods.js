const person = 'Alex'

//functions
const greet = () => 'hello'
let resultOne = greet();
console.log(resultOne);

//methods
let resultTwo = person.toUpperCase()
console.log(resultTwo);


/**
 * FUNCTION
 * A function is a block of code that does something specific. It can be invoked and run at any point in the code.
 *
 * Invoke function: greet()
 *
 * METHODS
 * A method is a function but is associated with an object or datatype AND is distingushed in the way we invoke them and where they are defined. Methods are invoked using dot notation
 *
 *Invoke method: person.toUpperCase()
 */


 /**
  * const person = 'Alex'

//funktioner
const hälsa = () => 'hej'
let resultatEtt = hälsa();
console.log(resultatEtt);

//metoder
let resultatTvå = person.toUpperCase()
console.log(resultatTvå);


/**
 * FUNKTION
 * En funktion är ett kodblock som gör något specifikt. Den kan anropas och köras när som helst i koden.
 *
 * Anropa funktion: hälsa()
 *
 * METODER
 * En metod är en funktion men är associerad med ett objekt eller en datatyp OCH skiljer sig i sättet vi anropar dem och var de är definierade. Metoder anropas med punktnotation.
 *
 * Anropa metod: person.toUpperCase()
 */

  */
